name: ci

on: pull_request

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: noop
        run: echo 'noop'

  pana:
    runs-on: ubuntu-latest
    steps:
      - name: noop
        run: echo 'noop'
